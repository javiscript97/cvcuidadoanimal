{% extends 'base_cpanel.html.twig' %}

{% block title %}Clinica Veterinaria!{% endblock %}

{% block body %}
<section class="px-24 py-5">
        <h1 class="shadtext text-3xl font-bold invisible sm:invisible md:visible pt-5 py-5">Veterinarios</h1>
        <div class="max-w-2xl mx-auto mb-6">
            <input
                type="text"
                id="search"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg"
                placeholder="Buscar Veterinario"
                >
        </div>
        <div class="overflow-x-auto  pb-24">
            <table  class="min-w-full bg-white border border-gray-300" id="pharmacyTab">
                <thead class="bg-gray-100">
                    <tr>
                        <th class="text-left py-3 px-4 font-semibold text-gray-600">ID</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-600">Nombre</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-600">Edad</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-600">Mail</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-600">Especialidad</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-600">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {% for vet in veterinarios %}
                    <tr class="border-t">
                        <td class="py-3 px-4">{{ vet.id }}</td>
                        <td class="py-3 px-4">{{ vet.nombre }}</td>
                        <td class="py-3 px-4">{{ vet.edad }}</td>
                        <td class="py-3 px-4">{{ vet.mail }}</td>
                        <td class="py-3 px-4">{{ vet.especialidad}}</td>
                        <td class="border border-gray-200 px-4 py-2">
                            <a href="{{ path('app_cpanel_vet_edit', { id: vet.id }) }}" class="text-yellow-500">Modificar</a>
                            <a href="{{ path('app_cpanel_vet_delete', { id: vet.id }) }}" class="text-red-500 ml-4" onclick="return confirm('¿Estás seguro de que deseas eliminarlo?');">Eliminar</a>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="6" class="text-center py-3">No hay Veterinarios registrados</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
</section>
<script src="{{ asset('js/searchfilter.js') }}"></script>
{% endblock %}